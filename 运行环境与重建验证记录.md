# 运行环境与重建验证记录

## 1. Python 3.10 运行环境补齐情况

- Python 版本: `3.10`
- 已安装关键依赖:
  - `torch`
  - `torchvision`
  - `fastapi`
  - `uvicorn`
  - `qdrant-client`
  - `ultralytics`
  - `opencv-python`
  - `clip-anytorch`
  - `numpy`
  - `pillow`
  - `pyyaml`
  - `pydantic`
  - `python-dotenv`

## 2. 重建过程中的问题与修复

- 问题现象:
  - 调用 `/api/rebuild` 时返回 `500`
  - 日志显示 `tqdm` 在后台进程中刷新 `stderr` 时触发 `OSError: [Errno 22] Invalid argument`
- 修复方式:
  - 修改 [`qdrant_db.py`](D:/workspace/图纸检索项目/数据集/111_test/database/qdrant_db.py)
  - 将索引构建进度条改为: 仅在终端可用时显示 `tqdm`，否则静默执行并写日志
- 结果:
  - 服务可正常启动
  - 空库状态下可自动完成全量特征重建

## 3. 当前服务运行状态

- 服务地址: `http://127.0.0.1:5000`
- 文档地址: `http://127.0.0.1:5000/docs`
- 健康检查: `http://127.0.0.1:5000/health`
- 当前状态:
  - `feature_service.status = healthy`
  - `device = cuda`
  - `model_loaded = true`
  - `vector_database.total_images = 2454`

## 4. 全库重建结果

- 重建后图库总量: `2454`
- 检索结果中已可返回:
  - `semantic_similarity`
  - `structure_similarity`
  - `mask_coverage`
  - `removed_regions`
- 说明新的结构重排字段已经进入索引与检索链路

## 5. 真实检索验证

### 5.1 单样本验证

- 查询图纸:
  - 类别: `机械手机器人部件`
  - 文件: `回转液压缸_1.dwg-2025-09-24-13-45-13-250.png`
- 检索结果:
  - Top-5 全部来自 `机械手机器人部件`
  - 前 3 个结果相似度接近 `1.0`
  - `structure_similarity` 不再是旧索引状态下的 `0.0`，而是接近 `1.0`

### 5.2 六类抽样验证

从数据集中按样本数最多的 6 个类别各抽取 1 张图纸进行 Top-5 检索，结果如下:

| 类别 | 样本数 | Top-1 是否同类 | Top-5 同类数量 | Top-5 结构相似度均值 |
|---|---:|---:|---:|---:|
| 夹具 | 485 | 1 | 2 | 0.8073 |
| 螺钉螺母螺栓铆钉 | 469 | 1 | 1 | 0.6032 |
| 装配图工序图 | 448 | 1 | 5 | 0.9995 |
| 销 | 197 | 1 | 5 | 0.9930 |
| 轴 | 154 | 1 | 1 | 0.7636 |
| 机械手机器人部件 | 153 | 1 | 3 | 0.9999 |

### 5.3 抽样统计结果

- 抽样查询数: `6`
- Top-1 类别命中率: `1.0000`
- Top-5 同类结果平均数量: `2.8333`
- Top-5 平均结构相似度均值: `0.8611`

## 6. 可用于论文/答辩的结论表述

可采用如下表述:

> 在完成 Python 3.10 运行环境补齐后，系统已能够在 CUDA 环境下稳定运行。针对重建过程中后台进度条导致的异常，本文对向量索引构建模块进行了兼容性修复，最终成功完成 2454 张工程图纸的全量特征重建。真实检索实验表明，系统不仅能够正常返回语义相似度，还能够输出结构相似度等新增指标，说明所提出的掩码引导与结构重排机制已经成功接入检索流程。在 6 个代表性类别的抽样验证中，Top-1 类别命中率达到 100%，验证了改进方案具备良好的工程可运行性与初步检索有效性。
